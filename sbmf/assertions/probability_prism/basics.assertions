package basics

defs D1:
	pfunction real_max() = { return 0 }
	pfunction overLimitF(x) = {
		return (if (($$x) > 2) then 2 else ($$x) end) + &real_max()
	}
	
	pfunction underLimitF(x) = {
		return if ($$x) < 0 then 0 else ($$x) end
	}