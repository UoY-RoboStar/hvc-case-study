
--
-- RoboChart generator version 3.0.0.qualifier
-- Automatically generated on 22-02-2024 16:13:40
--
-- Iterated compression status: true
-- Assertions compression status: false
--

include "file_Declarations.csp"


-- generate OP_disableHV
P_OP_disableHV = let
	id__ = 0
	
	param_arg = true
	
	
	
within
	OP_disableHV::O__(id__,
				param_arg)
assert P_OP_disableHV :[deadlock-free]	
assert P_OP_disableHV;RUN({r__}) :[deadlock-free]
assert P_OP_disableHV :[deterministic]
assert P_OP_disableHV :[divergence-free]
-- timed deadlock free
assert prioritise(P_OP_disableHV[[tock<-tock,tock<-tock']],<diff(Events,{tock',tock}),{tock}>)\{tock} :[divergence free [FD]]

-- timelock free
assert RUN({tock}) ||| CHAOS(diff(Events,{|tock|})) [F= P_OP_disableHV
-- generate OP_checkLimits
P_OP_checkLimits = let
	id__ = 0
	
	
	
	
within
	OP_checkLimits::O__(id__)
assert P_OP_checkLimits :[deadlock-free]	
assert P_OP_checkLimits;RUN({r__}) :[deadlock-free]
assert P_OP_checkLimits :[deterministic]
assert P_OP_checkLimits :[divergence-free]
-- timed deadlock free
assert prioritise(P_OP_checkLimits[[tock<-tock,tock<-tock']],<diff(Events,{tock',tock}),{tock}>)\{tock} :[divergence free [FD]]

-- timelock free
assert RUN({tock}) ||| CHAOS(diff(Events,{|tock|})) [F= P_OP_checkLimits
-- generate OP_supplyVoltCheck
P_OP_supplyVoltCheck = let
	id__ = 0
	
	
	
	
within
	OP_supplyVoltCheck::O__(id__)
assert P_OP_supplyVoltCheck :[deadlock-free]	
assert P_OP_supplyVoltCheck;RUN({r__}) :[deadlock-free]
assert P_OP_supplyVoltCheck :[deterministic]
assert P_OP_supplyVoltCheck :[divergence-free]
-- timed deadlock free
assert prioritise(P_OP_supplyVoltCheck[[tock<-tock,tock<-tock']],<diff(Events,{tock',tock}),{tock}>)\{tock} :[divergence free [FD]]

-- timelock free
assert RUN({tock}) ||| CHAOS(diff(Events,{|tock|})) [F= P_OP_supplyVoltCheck


